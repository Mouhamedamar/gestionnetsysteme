# Exemple pour docker-compose – copier en .env à la racine si besoin
# Les variables ci-dessous overrident les valeurs par défaut du compose.

# Base de données MySQL (service db)
# MYSQL_DATABASE=gestion_stock
# MYSQL_USER=gestion_stock
# MYSQL_PASSWORD=gestion_stock_secret
# MYSQL_ROOT_PASSWORD=root_secret

# Backend Django
# DJANGO_SECRET_KEY=your-secret-key
# DEBUG=0
# ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,backend
# CSRF_TRUSTED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# Frontend (utilisées au build de l’image)
# URL de l’API telle que vue par le navigateur (pas par le conteneur)
# VITE_API_URL=http://localhost:8000
# VITE_USE_API=true

# Email (optionnel, sinon console)
# EMAIL_BACKEND=console
# DEFAULT_FROM_EMAIL=noreply@local
